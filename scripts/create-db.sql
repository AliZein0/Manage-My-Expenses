-- Run this in MySQL Workbench or MySQL command line
CREATE DATABASE IF NOT EXISTS manage_my_expenses;

-- Optional: If you want to create a dedicated user (recommended for production)
-- CREATE USER 'expense_user'@'localhost' IDENTIFIED BY 'secure_password';
-- GRANT ALL PRIVILEGES ON manage_my_expenses.* TO 'expense_user'@'localhost';
-- FLUSH PRIVILEGES;